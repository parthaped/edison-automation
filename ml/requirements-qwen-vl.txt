# Qwen3-VL transcription for Amarel/local GPU runs. Install in a dedicated Qwen venv:
#   pip install -r ml/requirements-qwen-vl.txt
#
# Optional on Amarel for faster inference: install flash-attn separately and run with
# --attn-implementation flash_attention_2.

transformers>=4.57,<5
accelerate>=1.0
huggingface_hub>=0.23
pillow>=10.0
