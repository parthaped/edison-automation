# Qwen2.5-VL 7B local transcription (4-bit). Install on top of the CUDA ML venv:
#   pip install -r ml/requirements-qwen-vl.txt
#
# Windows: skip decord; qwen-vl-utils falls back to torchvision.

# Pin below 5.10: transformers 5.10+ imports torch.float8_e8m0fnu (needs torch>=2.7).
# Kraken venv uses torch 2.6+cu124 from setup_kraken_cuda.ps1.
transformers>=4.49,<5.10
accelerate>=1.0
qwen-vl-utils>=0.0.8
# 0.50+ fixes Params4bit _is_hf_initialized with transformers 5.x (patch in transcribe_qwen_vl until then).
bitsandbytes>=0.43
huggingface_hub>=0.23
pillow>=10.0
